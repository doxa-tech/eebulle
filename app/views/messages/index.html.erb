<% content_for :id, "messages" %>

<h1><%= t('message.index') %></h1>

<%= render 'category', messages: @preachings %>

<h2>Groupes de maison</h2>

<%= render 'category', messages: @homegroups %>

<h2>Discipulat</h2>

<%= render 'category', messages: @teachings %>

<h2>Études bibliques </h2>

<%= render 'category', messages: @bible_studies %>

