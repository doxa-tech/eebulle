<%= form_for [:admin, @gallery] do |f| %>	

	<%= render 'error_messages', object: f.object %>

	<%= f.label :name %>
	<%= f.text_field :name %>		

	<%= f.label :description %>
	<%= f.text_area :description %>

	<%= f.label :date %>
	<%= f.date_select :date %>
	
	<%= f.submit %>

<% end %>