<h2><%= @page.name %></h2>

<%= form_for [:admin, @page] do |f| %>

	<%= render 'error_messages', object: f.object %>

	<%= f.label :content %>
	<%= f.text_area :content, class: "ckeditor" %>
	
	<%= f.submit %>
<% end %>